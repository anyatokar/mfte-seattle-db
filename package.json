{
  "name": "mfte-simple-db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "csv-parser": "^3.2.0",
    "fast-csv": "^5.0.2",
    "firebase": "^10.7.0",
    "uuid": "^11.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "import-data": "npx tsx src/import.ts",
    "prettier": "prettier . -w",
    "check-websites": "npx tsx checkWebsites.ts",
    "compare": "npx tsx compare-csv.ts",
    "new": "npx tsx generateNewList.ts"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@types/node": "^22.14.1",
    "prettier": "3.2.5"
  }
}
